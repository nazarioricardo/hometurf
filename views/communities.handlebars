<div class="page" id="onboarding">
    
    <div class="jumbotron">
        <div class="container">
            <h2 class="col-md-6">Select your community</h2>
            <h4 class="col-md-6">If your community isn't registered contact us to see how to join our program</h4>
        </div>
    </div>
    
    <ul>
    {{#each communities}}
        <a href="/findSuperUnit/{{_id}}"><li>{{name}}</li></a>
    {{/each}}
    </ul>
</div>