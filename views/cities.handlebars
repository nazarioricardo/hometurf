<div class="page" id="onboarding">
    
    <div class="jumbotron">
        <div class="container">
            <h1>Select your city</h1>
        </div>
    </div>

    <ul>
    {{#each cities as |city|}}
        <a href="/findCommunity/{{city}}"><li>{{city}}</li></a>
    {{/each}}
    </ul>
</div>